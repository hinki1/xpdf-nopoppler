#!/bin/sh
set -e

if [ "$1" = "configure" ] || [ "$1" = "triggered" ] ; then
	if [ -e /usr/share/poppler/cMap ] ; then
		ln -sf /usr/share/xpdf/includes-poppler-yes /etc/xpdf/includes
	else
		ln -sf /usr/share/xpdf/includes-poppler-no /etc/xpdf/includes
	fi
fi

#DEBHELPER#
