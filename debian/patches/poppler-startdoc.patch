description: startDoc now takes a PDFDoc instead of an XRef
author : Andy Whitcroft <apw@ubuntu.com>

--- a/xpdf/PDFCore.cc
+++ b/xpdf/PDFCore.cc
@@ -201,7 +201,7 @@ int PDFCore::loadFile2(PDFDoc *newDoc) {
   }
   doc = newDoc;
   if (out) {
-    out->startDoc(doc->getXRef());
+    out->startDoc(newDoc);
   }
 
   // nothing displayed yet
