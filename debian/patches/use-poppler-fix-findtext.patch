Description: Upstream changes introduced in version 3.03-1
 This patch has been created by dpkg-source during the package build.
 Here's the last changelog entry, hopefully it gives details on why
 those changes were made:
 .
 xpdf (3.03-1) UNRELEASED; urgency=low
 .
   * New upstream release:
     - Refresh patches.
     - Drop 12-fix-typo-in-help.patch, fix-437725.patch, fix-479467.patch, and
       fix-577031.patch (now applied upstream).
   * Use -fpermissive build flag.
   * Really pass all arguments when using -m.
   * Fix broken -z argument (closes: #636390).
 .
 The person named in the Author field signed this changelog entry.
Author: Michael Gilbert <michael.s.gilbert@gmail.com>
Bug-Debian: http://bugs.debian.org/636390

---
The information above should follow the Patch Tagging Guidelines, please
checkout http://dep.debian.net/deps/dep3/ to learn about the format. Here
are templates for supplementary fields that you might want to add:

Origin: <vendor|upstream|other>, <url of original patch>
Bug: <url in upstream bugtracker>
Bug-Debian: http://bugs.debian.org/<bugnumber>
Bug-Ubuntu: https://launchpad.net/bugs/<bugnumber>
Forwarded: <no|not-needed|url proving that it has been forwarded>
Reviewed-By: <name and email of someone who approved the patch>
Last-Update: <YYYY-MM-DD>

--- xpdf-3.03.orig/xpdf/PDFCore.cc
+++ xpdf-3.03/xpdf/PDFCore.cc
@@ -1741,7 +1741,7 @@ GBool PDFCore::findU(Unicode *u, int len
     page = findPage(pg);
   }
   if (page->text->findText(u, len, startAtTop, gTrue, startAtLast, gFalse,
-			   caseSensitive, backward, wholeWord,
+			   caseSensitive, backward,
 			   &xMin, &yMin, &xMax, &yMax)) {
     goto found;
   }
@@ -1759,7 +1759,7 @@ GBool PDFCore::findU(Unicode *u, int len
 	 pg += backward ? -1 : 1) {
       doc->displayPage(textOut, pg, 72, 72, 0, gFalse, gTrue, gFalse);
       if (textOut->findText(u, len, gTrue, gTrue, gFalse, gFalse,
-			    caseSensitive, backward, wholeWord,
+			    caseSensitive, backward,
 			    &xMin, &yMin, &xMax, &yMax)) {
 	delete textOut;
 	goto foundPage;
@@ -1772,7 +1772,7 @@ GBool PDFCore::findU(Unicode *u, int len
 	 pg += backward ? -1 : 1) {
       doc->displayPage(textOut, pg, 72, 72, 0, gFalse, gTrue, gFalse);
       if (textOut->findText(u, len, gTrue, gTrue, gFalse, gFalse,
-			    caseSensitive, backward, wholeWord,
+			    caseSensitive, backward,
 			    &xMin, &yMin, &xMax, &yMax)) {
 	delete textOut;
 	goto foundPage;
@@ -1794,7 +1794,7 @@ GBool PDFCore::findU(Unicode *u, int len
       yMax = selectLRY;
     }
     if (page->text->findText(u, len, gTrue, gFalse, gFalse, stopAtLast,
-			     caseSensitive, backward, wholeWord,
+			     caseSensitive, backward,
 			     &xMin, &yMin, &xMax, &yMax)) {
       goto found;
     }
@@ -1811,7 +1811,7 @@ GBool PDFCore::findU(Unicode *u, int len
 	 gTrue);
   page = findPage(pg);
   if (!page->text->findText(u, len, gTrue, gTrue, gFalse, gFalse,
-			    caseSensitive, backward, wholeWord,
+			    caseSensitive, backward,
 			    &xMin, &yMin, &xMax, &yMax)) {
     // this can happen if coalescing is bad
     goto notFound;
